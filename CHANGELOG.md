# 更新日志

## 未发布

外部代码审计后的集中修复轮（2026-07-26），GameTest 从 88 项扩充到 90 项。

### 修复

- 奇点定义新增 `sync_singularities` S2C 同步：修复专用服务器上创造模式物品栏不显示奇点、以及先单机后联机时定义跨会话残留的问题。
- 无尽雨伞不再清除药水等其他来源的缓降效果；非潜行使用后正确消费交互，副手物品不再被同一次右键触发。
- 无尽鞘翅：飞行追踪改为带宽限窗口的时间戳记录，空中卸下/死亡/断线后不再误触发落地冲击；落地冲击不再伤害玩家。
- 无尽时钟：界面输入框按键不再穿透到容器快捷键；时间包服务端归一化到一天之内，输入框限制为纯数字。
- 幽匿/下界/末地合成台尊重原版配方的自定义剩余物（书本复制保留原书等）。
- 无尽箱：槽容量调整为 `Integer.MAX_VALUE-512` 并对旧存档钳制，修复接近上限时 shift 合并整型溢出导致整槽物品蒸发。
- 压缩机 GUI 数据槽改为 16 位拆分同步，`count`/`timeCost` 超过 32767 时进度显示不再错乱。
- 无尽弩装填 TNT 现在真正发射点燃的 TNT（含多重射击扇形角度与点燃音效）。
- 锤/毁灭者模式与自然荒芜之斧的范围改为对称闭区间（17×17 / 27×27），不再向 -X/-Z 偏移一格。
- 烈焰铲方块转化保留悬挂/朝向/含水等原状态属性。
- 创造模式破坏非空压缩箱/无尽箱时按潜影盒语义生成带内容组件的掉落物，不再静默销毁存储。
- 无尽箱/压缩箱界面在窗口逻辑分辨率不足时整体等比缩放并换算鼠标坐标，高 GUI Scale 下边缘槽位可见可点。
- `extreme_anvil_unbreak` 标签补齐中子收集器/压缩机全部八个家族方块。
- 补齐无尽盔甲 `humanoid_baby` 层贴图。

### 工程质量

- `validateResources` 新增 equipment 各层 `texture` 键与物品定义特殊模型 `base` 键的存在性校验。
- 时序敏感的 GameTest 实体断言改为 succeedWhen 轮询/延迟执行，大幅降低偶发误报。
- 根治 GameTest 结构区块实体加载竞态：测试在默认 8×8×8 结构包围盒外（相对坐标 8–12）操作实体时可能落入未强制加载的邻接区块（实体不 tick 或查询不到，约 10% 概率随机失败）。gametest 模组新增 mixin 把强制加载与就绪等待范围外扩 16 格，保证测试可触及的邻接区块在测试开始前达到 ENTITY_TICKING；连续 18 次全量套件零失败，详见 TEST_REPORT。
- 修正 FEATURE_MATRIX/INGAME_TEST_REPORT 中的 GameTest 计数与不存在的 `fake_*` 内部方块声明。

## 1.0.0-rc.1 - 2026-07-21

首个功能完整的 Minecraft Java 26.2 Fabric 发布候选版。

### 新增

- 完整材料成长链、14 个默认奇点与 13 个条件式科技奇点。
- 压缩工作台、二重压缩工作台、9×9 终极工作台及其有序/无序配方。
- 四级中子收集器、四级中子压缩机、压缩箱子与无尽箱子。
- 经典终局武器、工具、护甲、食物，以及功能矩阵中列出的 Re:Avaritia 扩展内容。
- 数据驱动奇点、压缩配方、双语资源与配方进度解锁链。
- OpenGL 与 Minecraft 26.2 实验性 Vulkan 后端兼容。

### 工程质量

- 88 个 GameTest 覆盖合成、使用、能力边界、保存、同步和机器性能。
- 构建期资源一致性门禁覆盖 568 个 JSON、96 个配方、27 个可掉落方块和 208 个双语键。
- 完成真实发行 JAR 的客户端、单人世界、专用服务器首次启动/重启及单客户端多人重连验证。

### 兼容性

- 仅支持 Minecraft Java 26.2、Java 25、Fabric Loader 0.19.3 与 Fabric API 0.155.2+26.2 基线。
- 使用新的 `avaritia26` 命名空间，不提供旧存档迁移。
