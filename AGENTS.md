# 开发约定

- 使用中文沟通和编写 Git commit message。
- 一个独立问题或功能点完成验证后，单独提交并推送；不要把无关修改混入同一提交。
- 以 Minecraft 26.2、Java 25、Fabric Loader 0.19.3、Fabric API 0.155.2+26.2 为固定基线。
- 核心只依赖 Fabric API，不引入多加载器架构或旧版兼容层。
- 编译通过不等于完成；物品与方块必须逐项验证合成、使用、保存、多人和专用服务器行为。
- 未完成客户端与专用服务器实际验证时，禁止宣称功能已完成。
